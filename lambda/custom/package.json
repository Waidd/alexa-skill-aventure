{
  "name": "skill-aventure",
  "version": "0.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc -p .",
    "build-test": "tsc -p ./test",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ask-sdk-core": "^2.0.0",
    "ask-sdk-model": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^10.5.4",
    "tslint": "^5.11.0",
    "typescript": "^2.9.2"
  }
}
